# プライバシーについて

## 通常の編集

プロジェクト、タイムライン、取り込んだ素材は利用者が選んだMac内の保存先に保存されます。本アプリ独自の分析目的で開発者へ素材を送信する機能はありません。

## 音声エンジン

音声生成では、利用者が接続した音声エンジンへ入力文章と音声設定を送信します。エンジンが同じMac内にある場合はローカル通信です。

AquesTalk Player連携では、同じMacに利用者がインストールした公式アプリへセリフ、プリセット名、一時WAVの保存先をコマンド引数として渡します。ネットワークへ送信する機能ではありません。アプリの保存先、利用条件の確認状態、任意の公開用ライセンスIDはmacOSのユーザー設定へ保存します。秘密のライセンスキーは取得・保存せず、AquesTalk Player側だけで扱います。

## ローカル／リモートAI

「このMac」を選んだ場合、選択した動画または音声から作成した圧縮音声、文字起こし結果、利用者の指示は、同じMacの`127.0.0.1`で動くAIノードだけへ渡され、ネットワークには送信されません。

「リモートMac」を選んだ場合、文字起こしでは圧縮音声を、重要箇所抽出では文字起こし結果と利用者の指示を、利用者が登録したAIノードへTailnet内のHTTPSで送信します。元の動画ファイル全体をそのまま送信する設計ではありません。

シナリオの「おすすめ配置をAIに依頼」を押した場合は、シナリオのセリフ本文、現在の話者名、登録キャラクター名、利用者の追加指示を選択中のAIノードへ送信します。ファイルそのもの、音声、画像、接続トークンはこの処理では送信しません。通常のシナリオ読み込みと確認だけでは送信しません。

絵コンテの「AI情報を書き出す…」は、利用者が選んだ保存先へJSONを作成します。アプリから外部サービスへ自動送信しません。JSONには台詞、キャラクター名、表情・プリセット名、素材の表示名と長さ、安定ID、配置情報が含まれます。素材ファイル本体、Mac内の保存先、取り込み元プロファイルの生データ、接続トークンは含みません。外部AIへ渡す場合は、利用者自身が内容と送信先の保存方針を確認してください。

AIノードの運用者、保存方針、接続先を利用者自身で確認してください。自分が管理していない接続先へ、個人情報・機密情報・第三者の非公開情報を送信しないでください。

## 認証情報

ローカル用とリモート用のAIノード接続トークンは別々のmacOSキーチェーン項目へ保存します。接続トークンと外部サービスのAPIキーはプロジェクトファイルには含めません。

## クレジット情報

素材に付属する`credit.json`、`credits.json`、`attribution.json`、README、LICENSEを読み取る場合があります。取得した情報はプロジェクト内のクレジット生成に利用します。
